# CLAUDE.md

本リポジトリ（およびこのテンプレートから作られた執筆リポジトリ）で
Claude が作業する際の必須事項を定める．

- 改良・機能追加の前に `docs/spec/` の要求要件文書を必ず読むこと．
- 依頼内容が spec と矛盾する場合は，実装前にユーザーへ確認すること．
- 組版の調整は `techbook-typesetting` Skill の規律に従う．
  測ってから詰める．並び替えは宣言してから行う．
- 版面の調整は `config/themes/bunko/theme.css` 冒頭の変数で行う．
  算出値（`--line-pitch` 以降）を直接書き換えない．
- 原稿（`src/chapters/`）は文芸の文体で書く．句読点は「、」「。」を使う．
  `docs/` と `README.md` は技術文書として「，」「．」で統一する．
- 原稿は `.textlintignore` で lint の対象から外している．
  技術文書向けの規則（文長・文体）が文芸の原稿に合わないためである．
  `docs/` と `README.md` は lint の対象である．
- 原稿へ HTML を書く範囲は最小に留める．
  クラス名は `theme.css` の「汎用クラス」節にあるものだけを使う．
- 見出しの先頭に絵文字を置く決まりのため，文書内リンクの断片は
  `#-機能` のようにハイフンで始まる．GitHub の描画に合わせる．
